# Minimal kafka-exporter configuration for the docker-compose smoke test.
# The entrypoint (entrypoint.sh) reads this block to build CLI flags for
# the kafka_exporter binary.  SASL credentials are appended by the
# entrypoint from the KAFKA_USER / KAFKA_PASSWORD environment variables.

kafka_exporter {
  kafka.server = "kafka:9092"
}
